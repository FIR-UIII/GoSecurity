### VulnAPP 
Written on Go and React via Claude (vibecoding). Main purpose is to learn codeql with provided vuln app as an source to codeql database

### Quickstart with app
```
make up   # build app via compose file and launch it via docker
make down # to stop and remove container
```