services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    environment:
      VITE_API_TARGET: http://backend:8080
    depends_on:
      - backend
    ports:
      - "5173:5173"
